apiVersion: v1
kind: Service
metadata:
  name: nexus-backend
  namespace: nexus-ai
spec:
  selector:
    app: nexus-backend
  ports:
    - name: http
      port: 80
      targetPort: 8787
      protocol: TCP
  type: ClusterIP
